main xml:

<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center">

    <TextView
        android:id="@+id/naglowekEkranu"
        android:text="Panel logowania"
        style="@style/NaglowekEkranu"/>

    <Button
        android:id="@+id/przycisk1"
        android:text="Przycisk 1"
        style="@style/PrzyciskGlowny"/>

    <Button
        android:id="@+id/przycisk2"
        android:text="Przycisk 2"
        style="@style/PrzyciskGlowny"/>

    <Button
        android:id="@+id/przycisk3"
        android:text="Anuluj"
        style="@style/PrzyciskGlowny.Anuluj"/>

    <EditText
        android:id="@+id/poleTekstowe"
        android:hint="nie wiem co"
        style="@style/PoleTekstowe"
        />

</LinearLayout>

nowy plik obramowanie_edittext.xml:

<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <stroke android:width="2dp" android:color="@color/PoleTekstowe"/>
    <corners android:radius="8dp"/>
</shape>

themes.xml:

<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.KonfiguratorPizzy" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
    </style>

    <style name="PrzyciskGlowny">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginStart">32dp</item>
        <item name="android:layout_marginEnd">32dp</item>
        <item name="android:layout_marginTop">16dp</item>
        <item name="android:padding">10dp</item>
        <item name="android:backgroundTint">@color/przycisk_niebieski</item>
        <item name="android:textColor">@color/przycisk_tekst_bialy</item>
        <item name="android:textSize">16dp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="NaglowekEkranu">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textSize">32dp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/naglowekEkranu</item>
        <item name="android:padding">10dp</item>
    </style>

    <style name="PrzyciskGlowny.Anuluj">
        <item name="android:backgroundTint">@color/przycisk_szary</item>
    </style>

    <style name="PoleTekstowe">
        <item name="android:background">@drawable/obramowanie_edittext</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textSize">16dp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColorHint">#9DFF00</item>
        <item name="android:padding">10dp</item>
    </style>

</resources>

colors.xml:

<?xml version="1.0" encoding="utf-8"?>
<resources>
    <color name="przycisk_niebieski">#0D47A1</color>
    <color name="przycisk_tekst_bialy">#FFFFFF</color>
    <!-- <color name="przycisk_czerwony">#E91E63</color> -->
    <color name="naglowekEkranu">#00FFFF</color>
    <color name="przycisk_szary">#808080</color>
    <color name="PoleTekstowe">#FF0000</color>
</resources>

java:

package com.example.konfiguratorpizzy;

import android.os.Bundle;
import android.view.View;
import android.widget.AdapterView;
import android.widget.ArrayAdapter;
import android.widget.Button;
import android.widget.CheckBox;
import android.widget.EditText;
import android.widget.RadioButton;
import android.widget.RadioGroup;
import android.widget.Spinner;
import android.widget.TextView;
import android.widget.Toast;

import androidx.activity.EdgeToEdge;
import androidx.appcompat.app.AppCompatActivity;
import androidx.core.graphics.Insets;
import androidx.core.view.ViewCompat;
import androidx.core.view.WindowInsetsCompat;

import java.util.ArrayList;

public class MainActivity extends AppCompatActivity {
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        EdgeToEdge.enable(this);
        setContentView(R.layout.activity_main);
        ViewCompat.setOnApplyWindowInsetsListener(findViewById(R.id.main), (v, insets) -> {
            Insets systemBars = insets.getInsets(WindowInsetsCompat.Type.systemBars());
            v.setPadding(systemBars.left, systemBars.top, systemBars.right, systemBars.bottom);
            return insets;
        });
    }

}


main xml:
<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="16dp"
    >

    <TextView
        android:id="@+id/textView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Nagłówek"
        android:textSize="24sp"/>
    <Button
        android:id="@+id/button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Główny Przycisk"
        style="PrzyciskNiestandardowy"/>
    <CheckBox
        android:id="@+id/checkbox"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Opcja 1"
        android:checked="true"/>
    <Switch
        android:id="@+id/switchCos"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Włącz coś"
        android:checked="true"/>

</LinearLayout>


main java:

package com.example.laboratoriummotyww;

import android.os.Bundle;

import androidx.activity.EdgeToEdge;
import androidx.appcompat.app.AppCompatActivity;
import android.widget.Button;
import android.content.Intent;

public class MainActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        EdgeToEdge.enable(this);
        setContentView(R.layout.activity_main);

        Button przyciskZapisz = findViewById(R.id.button); // Wstaw ID swojego przycisku
        przyciskZapisz.setOnClickListener(v -> {
            // Ten kod uruchamia drugą aktywność
            Intent intent = new Intent(MainActivity.this, DrugaActivity.class);
            startActivity(intent);
        });
    }
}

theme dla bialego ekranu:
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.LaboratoriumMotywów" parent="Theme.AppCompat.Light">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.LaboratoriumMotywów" parent="Base.Theme.LaboratoriumMotywów">
        <item name="colorPrimary">@color/brand_primary</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/brand_secondary</item>
        <item name="colorOnSecondary">@color/white</item>

        <!-- Dodajemy domyślne tło i kolor tekstu -->
        <item name="android:windowBackground">@color/background_light</item>
        <item name="android:textColor">@color/text_primary</item>
    </style>


</resources>

czerwone theme:
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->

    <style name="Theme.CzerwonyMotyw" parent="Theme.AppCompat.Light">
        <item name="colorPrimary">#B71C1C</item> <!-- Ciemnoczerwony -->
        <item name="android:windowBackground">#FFF9C4</item> <!-- Jasnożółty -->
        <item name="android:textColor">#000000</item> <!-- Czarny tekst -->
    </style>

</resources>

android mainfest:
<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LaboratoriumMotywów">
        <activity
            android:name=".DrugaActivity"
            android:exported="false"
            android:theme="@style/Theme.CzerwonyMotyw"
            />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>

theme dla czarnego ekranu:
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.LaboratoriumMotywów" parent="Theme.AppCompat.Light">
        <item name="android:windowBackground">@color/black</item>
        <item name="android:textColor">@color/white</item>
    </style>
    <style name="PrzyciskNiestandardowy">
        <item name="android:background">@color/red</item>
    </style>
</resources>

colors:
<?xml version="1.0" encoding="utf-8"?>
<resources>
    <color name="brand_primary">#0D47A1</color> <!-- Ciemnoniebieski -->
    <color name="brand_secondary">#FF6F00</color> <!-- Pomarańczowy -->
    <color name="text_primary">#212121</color> <!-- Ciemnoszary dla tekstu -->
    <color name="background_light">#FAFAFA</color> <!-- Jasnoszare tło -->
    <color name="white">#FFFFFF</color>
    <color name="black">#000000</color>
    <color name="red">#DE0000</color>
    <color name="darkOrange">#952A06</color>

</resources>

drugi xml:
<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".DrugaActivity">

</androidx.constraintlayout.widget.ConstraintLayout>

druga java:
package com.example.laboratoriummotyww;

import android.os.Bundle;

import androidx.activity.EdgeToEdge;
import androidx.appcompat.app.AppCompatActivity;
import androidx.core.graphics.Insets;
import androidx.core.view.ViewCompat;
import androidx.core.view.WindowInsetsCompat;

public class DrugaActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        EdgeToEdge.enable(this);
        setContentView(R.layout.activity_druga);
        ViewCompat.setOnApplyWindowInsetsListener(findViewById(R.id.main), (v, insets) -> {
            Insets systemBars = insets.getInsets(WindowInsetsCompat.Type.systemBars());
            v.setPadding(systemBars.left, systemBars.top, systemBars.right, systemBars.bottom);
            return insets;
        });
    }
}
